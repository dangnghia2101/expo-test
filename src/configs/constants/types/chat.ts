export const CHAT_TYPE = {
  TEXT: 'text',
  IMAGE: 'image',
  DELETED: 'deleted',
  USER_JOIN: 'user_join',
  USER_REMOVE: 'remove_user',
  USER_LEAVE: 'user_leave_group'
};

export const CHAT_CONTENT = {
  AI_ERROR: '[AI_ERROR]',
  DELETED: '[DELETED]',
  HIDDEN: '[HIDDEN]'
};
