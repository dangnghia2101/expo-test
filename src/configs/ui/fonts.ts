export const NAME = 'NotoSans';

const REGULAR = `${NAME}-Regular`;
const MEDIUM = `${NAME}-SemiBold`;
const BOLD = `${NAME}-Bold`;

export const Fonts = {
  REGULAR,
  MEDIUM,
  BOLD
};

export default Fonts;
